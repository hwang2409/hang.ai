services:
  searxng:
    image: searxng/searxng:latest
    ports:
      - "8888:8080"
    environment:
      - SEARXNG_SECRET=hang-searxng-local-dev-key-2026
    volumes:
      - ./searxng-settings.yml:/etc/searxng/settings.yml
      - ./searxng-limiter.toml:/etc/searxng/limiter.toml
